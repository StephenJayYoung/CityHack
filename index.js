 

 describe("Ers", function() {
 	// given an object, it looks for shared interests
 	it('looks for similarities between objects',  function() {
 	//reads the totalInterests file and looks for shared interests
 	var sharedInterests = require('..json');
 	expect(sharedInterests).to.eql(runner)
  });

var
var 
 });

var ERs = {
      "ER1": "runner",
      "ER2": "climber",
      "ER3": "musicMaker",
      "ER4": "biker"
      "ER5": "hiker",
      "ER6": "beerDrinker"
}